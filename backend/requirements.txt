Flask==3.0.3
Werkzeug==3.0.3
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1
python-dotenv==1.0.1

# Banco de dados: Cloud Firestore (Firebase)
firebase-admin>=6.5,<8

# Firestore em memória para desenvolvimento/testes sem credenciais (FIRESTORE_MOCK=1)
mock-firestore==0.11.0

# Servidor WSGI de produção (Render): gunicorn wsgi:app --bind 0.0.0.0:$PORT
gunicorn==22.0.0
